# vim: sts=2 ts=2 sw=2 et ai
# GENERATED BY SALT
{% from "freeradius/map.jinja" import freeradius with context %}
{% set default_conf = salt['pillar.get']('freeradius:'+configname, {}) %}

{%- for section in default_conf %}
{{ section }} {
  {%- for value in default_conf.get(section,[]) %} 
    {{ value }}
  {% endfor %}
}
{%- endfor -%}
